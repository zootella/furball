package org.zootella.furball.kind;

public enum Tile {

	/** Furball on grass. */
	fg,
	/** Furball on sink. */
	fs,
	/** Box on grass. */
	bg,
	/** Box on sink. */
	bs,
	/** Rock. */
	rk,
	/** Wall. */
	wl,
	/** Enemy. */
	en,
	/** Grass. */
	gs,
	/** Sink. */
	sk,
	/** Heart. */
	hr,
	/** Door closed. */
	dc,
	/** Door open. */
	dp,

	/** Outside, used to describe a position off the board. */
	os
}
